//! # Framework UI Layer
//!
//! Shared, game-agnostic UI utilities: screen fading, theming,
//! and reusable widget infrastructure.

/// Screen fade/transition system.
pub mod fading;

/// Design tokens: colors, fonts, sizes.
pub mod theme;

/// Reusable UI widgets (buttons, sliders, dropdowns).
pub mod widgets;
